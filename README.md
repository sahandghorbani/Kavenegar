To run the JSON Server alongside your Next.js development server, execute the following command:

```bash
json-server db.json --watch --port 3001

npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
